---
name: cap
description: Shorthand for /forge capture — register a practice or insight in practices/inbox/
---

Run `/forge capture` with $ARGUMENTS. Pass-through alias to reduce friction.

If $ARGUMENTS is empty, run `/forge capture` with no arguments (auto-detect mode).
