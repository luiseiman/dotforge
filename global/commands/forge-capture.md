---
name: forge-capture
description: Register a practice or insight in practices/inbox/ (pass description as argument)
---

Run `/forge capture` with the description from $ARGUMENTS. Creates a new practice file in `$DOTFORGE_DIR/practices/inbox/` with YAML frontmatter. Example: `/forge-capture "hooks should ignore migration files"`
